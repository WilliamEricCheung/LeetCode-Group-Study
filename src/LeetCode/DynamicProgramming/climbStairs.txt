class Solution:
    def climbStairs(self, n: int) -> int:
        num = dict()
        num[1] = 1
        num[2] = 2
        if n <= 0:
            return 0
        if n > 2:
            for n in range(3,n+1):
                num[n] = num[n-1] + num[n-2]
        return num[n]
