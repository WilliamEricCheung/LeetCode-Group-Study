package tech.wec.SQL;

public class RisingTemperature_197 {
    /*
    select weather.id as 'Id'
from weather join weather as w on datediff(weather.RecordDate, w.RecordDate) = 1
and weather.Temperature > w.Temperature
     */
}
